class Solution {
public:
    bool isValid(string s) {
        int sSize = s.size();
        for(int i=0; i < sSize; i++)
        {
            switch (s[i])
            {
            case 
                break;
            
            default:
                break;
            }
        }
    }
};